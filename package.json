{
  "name": "akira-saas-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd akira-saas && npm run dev",
    "build": "cd akira-saas && npm install --legacy-peer-deps && npm run build",
    "preview": "cd akira-saas && npm run preview"
  }
}
