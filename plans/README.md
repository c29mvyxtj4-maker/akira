# Animation plans

Generated by `improve-animations` against commit `8b6d5ce`. Each plan is
self-contained — an executor (any agent/model) can implement it with zero context
from the audit conversation. Plans only describe motion changes; they do not modify
source themselves.

| # | Plan | Severity | Status |
|---|------|----------|--------|
| 001 | [Command Palette exit animation](001-commandpalette-exit-animation.md) | HIGH | TODO |
| 002 | [Scope `transition: all` on nav & buttons](002-scope-transition-all-on-nav-and-buttons.md) | MEDIUM | TODO |
| 003 | [Stop animating `width: auto` on sidebar labels](003-sidebar-avoid-animating-width-auto.md) | MEDIUM | TODO |

## Recommended execution order

1. **001** — highest leverage, fully isolated (one file, no side effects).
2. **002** — trivial CSS scoping, no behavior change; safe to batch with 001.
3. **003** — do last; it is the only one with a visible behavior change and a
   required feel-check.

## Dependencies

- None. All three touch different files (`CommandPalette.jsx`, `index.css`,
  `Sidebar.jsx`) and can be implemented and reviewed independently or in one pass.

## Not included (vetted out)

- `globals.css:217` `.transition-all` — a utility named `transition-all` is `all` by
  design; not a defect.
- `Sidebar.jsx:163` submenu `height: 0 ↔ auto` accordion — accepted pattern; would be
  its own plan if ever revisited.
- Modal, Tooltip, KpiCard, Toast entrances — already correct (exit animations, scoped
  transitions, `MotionConfig reducedMotion="user"` in `main.jsx`).

## To implement

Run each plan with any agent, or via `improve-animations execute plans/001-…md`.
After implementing, update the Status column here (TODO → DONE) or run
`improve-animations reconcile`.
