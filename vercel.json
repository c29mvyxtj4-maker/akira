{
  "buildCommand": "cd akira-saas && npm install --legacy-peer-deps && npm run build && echo 'Build complete'",
  "outputDirectory": "akira-saas/dist",
  "trailingSlash": false,
  "routes": [
    {
      "src": "^/(?!assets|icons).*",
      "dest": "/index.html"
    }
  ]
}
