{
  "buildCommand": "cd akira-saas && npm install --legacy-peer-deps && npm run build && cp -r dist/* .. && cp -r dist/.[^.]* .. 2>/dev/null || true",
  "outputDirectory": ".",
  "trailingSlash": false,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
