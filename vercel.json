{
  "buildCommand": "cd akira-saas && npm install --legacy-peer-deps && npm run build && cd .. && cp -r akira-saas/dist/* . && cp -r akira-saas/dist/.[^.]* . 2>/dev/null || true",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
